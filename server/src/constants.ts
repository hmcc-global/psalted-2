export const JWT_MIN_AGE = 24 * 60 * 60; // token stays for 1 day (1 sec increment)
export const JWT_MAX_AGE = 24 * 60 * 60 * 7 * 8; // token stays for 2 month (1 sec increment)
export const PWD_RESET_TOKEN_TTL = 60 * 60 * 1000; // 1 hour in milliseconds
