type SongEditorFields = {
  artist: string;
  title: string;
  themes: Array<string>;
  tempo: Array<string>;
  year: number;
  code: string;
  originalKey: string;
  recommendedKeys: Array<string>;
  chordLyrics: string;
};

export type { SongEditorFields };
